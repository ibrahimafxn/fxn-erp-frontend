export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5000/api', // URL de ton backend dev
  // tu peux ajouter d'autres variables ici, ex: analytics, feature flags
  featureFlags: {
    enableDebug: true
  }
};
